## v0.2.1 - 2026-08-31

- 上一版本: v0.2.0
- 发布日期: 2026-08-31

### 变更

- 30e1a6d7 docs: rewrite deployment guide (offline bundle flow) and add development guide with root package.json
- c38fca8c docs(readme): add Option A single-process quick start to all locales
- 4bd377cb fix(deploy): write install.sh with LF endings and auth redis check in verify script
- 38116390 fix(web): pin date-fns to 2.30.0 in classic theme to fix build

### 部署备注

- 部署脚本修复：install.sh 改为 LF 行尾写入（此前 Set-Content 在 Windows 写 CRLF，服务器 bash 报 "invalid option name: pipefail"）；verify-services.sh 的 redis-ping 检查改为从 .env.production 读取 REDIS_PASSWORD（此前裸 ping 误报 NOAUTH）
- 数据库无结构变更，AutoMigrate 无需手动 SQL
- 注意：v0.2.0 发版时 scripts/versions/v0.2.0.md 未生成（构建脚本当时缺少该逻辑），本版本起恢复版本文件记录
